# CITY OS · 深圳内涝项目总纲 v3.2

> 当前状态：守恒灰箱基线已实现并通过数值/接口测试；真实独立事件不足，尚未证明业务预测技巧。

## 1. 目标

系统围绕 SENSE → UNDERSTAND → PREDICT → SIMULATE → DECIDE → ACT，回答三个问题：

1. 在给定预报签发时点，未来各区代表性积水深度如何演化？
2. 降雨、潮位、排水和泵站状态改变后，风险如何变化？
3. 哪些结论来自观测，哪些来自 GIS 推导、参数先验或模拟？

当前空间尺度为深圳 10 个行政区、小时步长。约 2km/500m 和街道点结果是受 GIS 约束的排序下尺度，不是二维水动力求解。

## 2. 核心架构

```text
预报签发快照 + 新鲜水位 + DEM/WorldCover/OSM
                    │
                    ▼
       规范初始分析与 64 成员参数集合
       （前24个完整小时地表存水 spin-up + 可用观测）
                    │
                    ▼
  守恒图状态转移（体积 m³，小时同步更新）
   ├─ 降雨产流
   ├─ 排水与潮位顶托
   ├─ 行政区下坡路由
   └─ 河道/城市边界外排
                    │
                    ▼
 两段蓄水曲线 → P10/P50/P90 水深 + 超阈频率
        ├─ PREDICT
        ├─ SIMULATE（成对反事实）
        ├─ STREET / GRID（有界下尺度）
        └─ DECIDE / ACT
```

预测与推演不再依赖物理教师合成标签训练的 LSTM。旧模型保留在 `backend/app/model.py`、`backend/app/lstm.py` 和 `ml/`，仅用于历史实验复现，生产导入图不会加载它们。

从系统层看，它可抽象为受控 POMDP / WAM：静态城市结构 `G` 约束状态，积水体积集合为
`x`，降雨与潮位为环境强迫 `u`，泵闸/排水为动作 `a`，传感器为观测 `y`，积水、暴露、
道路中断与能耗构成成本 `J`。当前已在动力学、同化和成对反事实之上实现安全建议模式的
有限时域鲁棒 CEM：为十区搜索一组时域内常值保持的排水控制倍率，经边界、变化率、应急增排
预算和 16 成员无恶化护栏投影后输出建议与可回放审计。它不是一次调用内优化动作序列的严格
MPC，也不是已经训练或部署的强化学习策略；控制升级顺序为 CEM 基线 → 约束 MPC / 离线或
残差 RL 影子评估 → 仅低风险动作的有限闭环。

## 3. 当前数据

已纳入：

- Open-Meteo 多点确定性小时降雨预报；失败时明确标注合成回退强迫；
- 480 个 DEM 点、19,968 个 WorldCover 不透水网格、28,625 路段、5,965 水系要素；
- 206 个历史易涝点、148 个站点特征；
- 6,573 条质控小时水位。148 站映射为 102 个站码区段、45 个修复区界包含、1 个最近标注易涝点回退；
- CHIRPS 少量历史逐日降雨，仅作事件检索/背景核验；
- 未校准调和潮和参数化风暴增水代理。

尚缺且优先级最高：

1. 多年真实积涝深度与事件台账；
2. 深圳 5 分钟自动站雨量、6 分钟雷达及历史 forecast-as-issued；
3. 本地潮位、河道/水库水位及统一高程基准；
4. 子汇水区、管网、排口、泵曲线、闸门和运行日志；
5. 亚米级裸地 DEM、道路横坡和下穿设施。

来源、许可、字段和接入顺序见 `docs/data_source_plan.md`。

## 4. 模型与不确定性

主状态是每区地表存水体积。每小时执行：

```text
S(t+1) = S(t) + runoff + routed_in
         - routed_out - drainage - external_outflow
```

水深由 0.15m 连续断点的两段式蓄水曲线反演。集合扰动六类参数：产流系数、有效排水能力、浅层洼地面积、扩展受淹面积、路由速度和外排率。

新鲜水位不会硬覆盖整个行政区，而是通过带 0.10m 代表性误差的确定性局地 EnSRF 更新初始集合。同化体积增量单独记账，不伪装成物理入流。

## 5. 可复现性与接口语义

- `forecast_run_id` 标识一次带签发时间的强迫快照；
- 首个消费者把观测截止时间、初始分析和参数成员冻结在该快照；
- `model_run_id` 还包含初态、潮位、控制量和参数集合哈希；
- `simulation_run_id` 还包含情景、成员数和基线/情景模型运行；
- 所有缓存有容量上限，栅格分辨率和预报时长有 API 边界；
- 第一项时间序列是“首个未来有效时次”，不是当前实况。

## 6. 验证边界

当前可以证明：

- 逐节点/全城质量守恒；
- 非负、顺序无关；
- 更强排水和外排不会使峰值变坏；
- 更高潮位不会改善重力排水；
- EnSRF 均值向观测移动且离散度收缩；
- 预测、推演、街道和网格共享规范基线。

当前不能证明：

- 真实事件 AUC、Brier、CSI、命中率或漏报率；
- 街道/道路级工程水深；
- “优于 LSTM/Transformer”；
- 超阈集合频率已经校准为真实概率。

## 7. 下一阶段

1. 建立只追加数据湖，持续归档积涝水位、自动站、雷达和预报签发版本。
2. 人工核验独立事件，冻结按事件划分的测试集。
3. 用事件回放校准灰箱参数，并与零积水、持续性和简单消退基线比较。
4. 接入气象集合，把降雨落区/峰值/时序不确定性传播到水深集合。
5. 取得管网和微地形后升级为子汇水区—管网图，离线耦合 SWMM/1D–2D。
6. 只有在独立事件足够后，才加入参数/残差学习、GNN 状态空间或神经算子代理。
7. 按 `Gymnasium → 奖励与硬约束 → MPC/CEM 基线 → 离线/残差 RL → 影子运行 → 建议模式 → 有限闭环`
   演进自主动作优化，高风险动作长期保留人工审批。
