# 外部平台凭据配置

复制项目根目录的 `.env.example` 为 `.env`，只在本机填写深圳开放数据平台会话和天地图 Key。

- `.env` 已被 Git 忽略，禁止提交真实 Cookie、Key 或下载链接。
- 登录会话会过期；接口返回鉴权错误时应重新获取，而不是把凭据写入源码。
- 共享数据时只提交来源编号、获取日期、许可和校验值。
- CI 和部署环境应通过密钥管理服务注入环境变量。

未配置凭据时，实时平台接口必须明确返回不可用状态或使用标记为缓存的数据，不能把回退样例标记为实时观测。
